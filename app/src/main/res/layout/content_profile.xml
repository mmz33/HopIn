<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="aub.hopin.Profile"
    tools:showIn="@layout/activity_profile">

    <ImageView
        android:layout_width="170dp"
        android:layout_height="170dp"
        android:id="@+id/profie_image_view"
        android:src="@drawable/missing_profile"
        android:layout_below="@+id/profile_title"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="100dp"
        android:layout_height="60dp"
        android:text="@string/upload"
        android:id="@+id/profile_upload_button"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/profile_status_edit_text"
        android:inputType="text"
        android:editable="true"
        android:text="Hello! I am using HopIn"
        android:layout_below="@+id/profile_upload_button"
        android:layout_centerHorizontal="true" />

    <RadioGroup
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/profile_switch_user_type_radio_group"
        android:layout_below="@+id/profile_switch_user_type"
        android:layout_alignParentStart="true">

        <RadioButton
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/passenger"
            android:id="@+id/profile_passenger_radio_button"
            android:checked="false"
            android:layout_below="@+id/profile_switch_user_type"
            android:layout_alignParentStart="true" />

        <RadioButton
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/driver"
            android:id="@+id/profile_driver_radio_button"
            android:checked="false" />
    </RadioGroup>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/switch_user_type"
        android:id="@+id/profile_switch_user_type"
        android:textSize="20dp"
        android:layout_below="@+id/profile_status_edit_text"
        android:layout_alignParentStart="true"
        android:layout_marginTop="36dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Profile"
        android:id="@+id/profile_title"
        android:textSize="35dp"
        android:layout_alignParentTop="true"
        android:layout_alignEnd="@+id/profile_upload_button" />

</RelativeLayout>
